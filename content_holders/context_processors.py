# Stub context processor to prevent import errors
# This is no longer used - content holders are now managed through pages app as snippets
# and loaded via template tags

def header_content(request):
    """Stub function - no longer used"""
    return {}

def footer_content(request):
    """Stub function - no longer used"""
    return {}
